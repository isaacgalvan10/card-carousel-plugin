import { registerBlockType } from '@wordpress/blocks';

registerBlockType('em_cards/cards', {
  title: 'EM Card Carousel',
  description: 'Card carousel',
  icon: 'format-image',
  category: 'layout',
});
